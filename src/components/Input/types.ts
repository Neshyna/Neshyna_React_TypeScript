export interface InputProps {
  name: string;
  type: string;
  // or type: "text" | "password" | "email" | "number" | "checkbox" | "radio"  etc...
  placeholder: string;
  label: string;
  id: string;
}
